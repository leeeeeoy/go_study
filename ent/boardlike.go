// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/leeeeeoy/go_study/ent/board"
	"github.com/leeeeeoy/go_study/ent/boardlike"
	"github.com/leeeeeoy/go_study/ent/user"
)

// BoardLike is the model entity for the BoardLike schema.
type BoardLike struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// BoardID holds the value of the "board_id" field.
	BoardID int `json:"board_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BoardLikeQuery when eager-loading is set.
	Edges        BoardLikeEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BoardLikeEdges holds the relations/edges for other nodes in the graph.
type BoardLikeEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Board holds the value of the board edge.
	Board *Board `json:"board,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BoardLikeEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.User == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// BoardOrErr returns the Board value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BoardLikeEdges) BoardOrErr() (*Board, error) {
	if e.loadedTypes[1] {
		if e.Board == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: board.Label}
		}
		return e.Board, nil
	}
	return nil, &NotLoadedError{edge: "board"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BoardLike) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case boardlike.FieldID, boardlike.FieldUserID, boardlike.FieldBoardID:
			values[i] = new(sql.NullInt64)
		case boardlike.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BoardLike fields.
func (bl *BoardLike) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case boardlike.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			bl.ID = int(value.Int64)
		case boardlike.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				bl.UserID = int(value.Int64)
			}
		case boardlike.FieldBoardID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field board_id", values[i])
			} else if value.Valid {
				bl.BoardID = int(value.Int64)
			}
		case boardlike.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				bl.CreatedAt = value.Time
			}
		default:
			bl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BoardLike.
// This includes values selected through modifiers, order, etc.
func (bl *BoardLike) Value(name string) (ent.Value, error) {
	return bl.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the BoardLike entity.
func (bl *BoardLike) QueryUser() *UserQuery {
	return NewBoardLikeClient(bl.config).QueryUser(bl)
}

// QueryBoard queries the "board" edge of the BoardLike entity.
func (bl *BoardLike) QueryBoard() *BoardQuery {
	return NewBoardLikeClient(bl.config).QueryBoard(bl)
}

// Update returns a builder for updating this BoardLike.
// Note that you need to call BoardLike.Unwrap() before calling this method if this BoardLike
// was returned from a transaction, and the transaction was committed or rolled back.
func (bl *BoardLike) Update() *BoardLikeUpdateOne {
	return NewBoardLikeClient(bl.config).UpdateOne(bl)
}

// Unwrap unwraps the BoardLike entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bl *BoardLike) Unwrap() *BoardLike {
	_tx, ok := bl.config.driver.(*txDriver)
	if !ok {
		panic("ent: BoardLike is not a transactional entity")
	}
	bl.config.driver = _tx.drv
	return bl
}

// String implements the fmt.Stringer.
func (bl *BoardLike) String() string {
	var builder strings.Builder
	builder.WriteString("BoardLike(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bl.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", bl.UserID))
	builder.WriteString(", ")
	builder.WriteString("board_id=")
	builder.WriteString(fmt.Sprintf("%v", bl.BoardID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(bl.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BoardLikes is a parsable slice of BoardLike.
type BoardLikes []*BoardLike
